Built-in demo traffic clips used by the app in Simulation mode.

These files are lightweight demo sources prepared from larger traffic videos so the project
can run immediately without requiring users to upload footage first.
